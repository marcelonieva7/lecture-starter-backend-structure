const { event, eventScore } = require('./event');
const { bet } = require('./bet');
const { user, userId, updateUser } = require('./user');
const { transaction } = require('./transaction');

module.exports = {
  event,
  eventScore,
  bet,
  user,
  userId,
  updateUser,
  transaction
};
